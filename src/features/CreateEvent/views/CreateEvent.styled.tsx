import styled from 'styled-components/native';
import theme from '../../../shared/theme/theme';

export const ReturnButton = styled.TouchableHighlight`
  background-color: ${theme.palette.blue};
  border-radius: 100px;
  width: 56px;
  height: 56px;
  justify-content: center;
  align-items: center;
`;
